#functions-
instead of copy pasting a loop every time you need to search a list, you can define a function using the def keyword.

#local vs global variables-
Variables created inside a function live in a different world than variables created outside of it. You will explore local scope (variables that only exist while the function is running) and global scope (variables accessible anywhere). Mastering this prevents hard-to-track bugs where functions accidentally overwrite data. 