#Variable 
 it is the space where we store the values and the variable name start form the character .. it can store the values of different data types like integer, float, boolean etc. 

 #datatypes

int= it store the integer values
float = it store the decimal values
bool = it store the boolean value i.e True and False.

#dynamic Typing 
Detect the type of the datatype 

#type()
it is used to check the datatype of the variable.