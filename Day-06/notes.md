#list - A list is a collection of multiple items stored in a single variable. Lists are ordered and mutable(can be changed).

#list indexing and slicing-
first index = 0
last index = n-1
last index=-1

slicing- list[start:end:steps]

#methods of list -
1- .append(10)
2- .insert(index,item)
3- .pop(index)
4- .len(list)