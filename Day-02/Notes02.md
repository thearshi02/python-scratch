#inpur function():
this is used to take the input from the user. It always take the input in the string format but we to typecast it according to our necessity.

#typecasting(Conversion):
typecasting means to convert one datatype into other datatype.

#string concatenation:
it means to add string into one another.

#F-String(Formatted String):
clean way to inject variables directly into the text by placinf an f before the quotes and wrapping variables into curly braces: f"Hello,{name}"