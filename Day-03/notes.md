#Comparison Operator
These are the operators which are used to compare the values. it return the boolean value(True False). 
== equals to
!= not eqauls tp
< ,> less than and greater than
<=  ,>= less than equal to or greater than equal to 

#if statement 
It is the most basic way to control the flow of program. code inside an if block only runs if the condition is true. 

#handling alternatives(elif and else)
Allows you to check multiple conditions in sequence if the previous ones are false
